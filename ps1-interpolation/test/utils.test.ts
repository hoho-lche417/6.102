/**
 * utils.test.ts
 *
 * Tests for helper functions you add to src/utils.ts.
 *
 * If a helper function is complex enough to deserve its own tests, it
 * belongs in utils.ts — and its tests belong here.
 *
 * Simple helpers (one or two lines) that are only used in one module don't
 * need to be in utils.ts; they can live unexported in that module.
 */

// import assert from 'assert';
// import { assertApproxEqual } from '../src/utils';
// import { yourHelper } from '../src/utils';

// ── Your tests here ───────────────────────────────────────────────────────────
